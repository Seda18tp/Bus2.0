# Configurar estas variables en Vercel > Settings > Environment Variables
# (Conexión directa de Supabase, Settings > Database > Connection string)

APP_ENV=production

DB_HOST=db.xxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASS=tu_password_super_segura
